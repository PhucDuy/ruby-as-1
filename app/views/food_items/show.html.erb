<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-md-3">
    <%= image_tag @food_item.image_url, :class => "img-thumbnail"%>
  </div>
  <div class="col-md-4">
    <h4><%= @food_item.name %></h4>
    <p ><%= @food_item.description %></p>
    <p ><%= number_to_currency(@food_item.price) %></p>
  </div>
</div>
<div class="row margin_top_bottom">
  <div class="col-md-4">
    <%= link_to 'Back',menu_path,class: "btn btn-primary" %>
  </div>
</div>